<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sp.smstock.mapper.StockMapper">

    <update id="deduct">
        UPDATE sm_stock SET stock_num = stock_num - #{stockNum} WHERE product_id = #{productId} AND stock_num >= #{stockNum}
    </update>
</mapper>